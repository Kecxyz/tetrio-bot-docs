# `"type": "targets"`

> ## WIP

## Formats

* (string) `type`: `"targets"`
  * (obj) `data`:
    * (string) `id`:
    * (int) `frame`:
    * (string) `type`:
    * (string[]) `data`:

TODO: more testing
